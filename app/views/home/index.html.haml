= render "shared/navbar", logo: asset_path("logo_dark.png")
= render "shared/flash"

= react_component("HomePage",
  { initial_results: {results: []},
    most_recent: @results[:most_recent][:results],
    favorites: @results[:favorites][:results],
    search_url: celebrities_path,
    t: component_translations("celebrity_list")})
